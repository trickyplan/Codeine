<?php

    /* OSWA Codeine
     * @author BreathLess
     * @type Codeine Driver
     * @description: Base Model Create
     * @package Codeine
     * @subpackage Drivers
     * @version 5.0
     * @date 06.12.10
     * @time 23:15
     */

    self::Fn('Create', function ($Call)
    {
        $Data = array();
        $Model = array();

        foreach ($Model['Nodes'] as $Name => $Node)
        {
            if (isset($Data['Name']))
            {
                
            }
        }
    });